const {Runtime}=require('../electron/runtime.cjs'),{resolve,join}=require('node:path')
;(async()=>{const r=new Runtime(resolve('resources/runtime'),(process.env.SERVERLOOM_TEST_RUNTIME_HOME || join(process.env.APPDATA,'serverloom','codex')));try{await r.start();const result=await r.request('account/rateLimits/read',{});const buckets=result.rateLimitsByLimitId||{default:result.rateLimits};console.log(JSON.stringify(Object.fromEntries(Object.entries(buckets).map(([key,value])=>[key,{primary:value?.primary,secondary:value?.secondary}]))))}finally{r.stop()}})().catch(e=>{console.error(e.message);process.exitCode=1})
